name: Test Exam Pages
on:
  push:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check all exam pages
        run: |
          for url in \
            "https://rishi-ewh.pages.dev/exam.html" \
            "https://rishi-ewh.pages.dev/topic-exam.html" \
            "https://rishi-ewh.pages.dev/sampurna-pariksha.html"
          do
            status=$(curl -L -o /dev/null -s -w "%{http_code}" "$url")
            echo "$status $url"
            [ "$status" != "200" ] && exit 1
          done
          exit 0
